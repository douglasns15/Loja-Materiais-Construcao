{
  "name": "@nexoloja/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "predeploy": "npx prisma generate --schema ../../packages/db/prisma/schema.prisma",
    "deploy": "wrangler deploy",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "@nexoloja/core": "*",
    "@nexoloja/db": "*",
    "@nexoloja/shared": "*",
    "hono": "^4.6.14",
    "jose": "^5.9.6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241218.0",
    "wrangler": "^4.107.0"
  }
}
